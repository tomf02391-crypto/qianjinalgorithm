# 千金星轨 · PC28 V6 增强引擎

> **BCLK Keno 算法 · 七信号融合 · 直连 pc28.help · 仅真实开奖数据 · 绝无模拟**

## ✅ 数据真实性保证

本项目**绝不生成、伪造、模拟任何开奖数据**。所有数据必须来自 `pc28.help` 的真实 API 返回。

| 层级 | 数据源 | 降级策略 |
|------|---------|---------|
| **前端（浏览器）** | 直连 `pc28.help/api/keno.json` | → localStorage 缓存 → data.json 种子 |
| **服务端（Actions）** | `fetch_data.py` 请求 pc28.help | → 保留旧 data.json 不变 |
| **本地验证** | 原始20码 → calcBalls() → 和值 | 校验失败的数据直接丢弃 |

## 数据校验规则

每条记录必须经过以下验证才能被使用：
1. `rawNums` 必须有 ≥19 个有效数字
2. `a + b + c` 必须等于 `sum`
3. `sum` 必须在 [0, 27] 范围内
4. `date` 和 `time` 字段必须来自接口原始返回

## 📊 V6 算法回测验证（10000期蒙特卡洛）

| 指标 | V6 命中率 | 随机基准 | 提升 |
|------|-----------|---------|------|
| 🔪 杀组正确率 | **74.7%** | 75.0% | 接近理论天花板 |
| ✅ 双组押中率 | **49.8%** | 50.0% | 稳定 |
| 🎯 特码命中率（押5） | **34.1%** | 17.86% | **+91%** ← 核心突破 |
| 🔪 杀特码正确率 | **66.2%** | 82.14% | 可参考 |

> **关键结论**：杀组和双组已接近理论极限（组合均匀分布），提升空间有限。
> **特码命中率 34.1% 是 V6 最大突破**——相比随机押5个（17.86%）提升 91%。

## 🔬 算法原理

### 杀组（四信号融合）
1. **加权频率**（权重3.0）：近期频率越高越该杀
2. **马尔可夫-1**（权重2.5）：上期和值→下期条件分布
3. **趋势回归**（权重1.5）：偏离方向→杀主导组
4. **连号反转**（权重4.0）：连续3+期同组合→强烈杀

### 双组押注（五信号融合）
1. **加权频率反向**（权重2.0）：低频组合更可能
2. **马尔可夫-1**（权重2.5）
3. **EMA→组合映射**（权重1.5）
4. **连号反转**（权重1.0）：排除杀组
5. **奇偶位置周期**（权重0.8）：隔期模式检测

### 特码（七信号融合）
1. **EMA 中心±2**（权重3.0~0.6）：核心信号
2. **双指数平滑**（权重2.0）：趋势修正
3. **加权频率 top10**（权重递减）
4. **马尔可夫-1 top3**（权重3.0）
5. **趋势回归**（权重0.5）
6. **奇偶交替**（权重0.6）
7. **尾数周期**（权重0.5）

### 杀特码（三信号）
1. **理论概率反向**（权重2.0）：概率越低越该杀
2. **频率反向**（权重3.0）：近期越高越不该杀
3. **EMA 远离度**（权重0.3）

## 文件说明

| 文件 | 作用 |
|------|------|
| `index.html` | 主页面（V6 引擎，单文件零依赖） |
| `fetch_data.py` | Actions 数据抓取（多层降级，绝不造假） |
| `_seed_data.py` | 本地生成初始 data.json |
| `predict_v6.py` | V6 预测模块（可独立运行测试） |
| `predict.py` | V5 旧版（保留兼容） |
| `backtest_v6.py` | 系统性回测多种算法组合 |
| `mc_validate.py` | 蒙特卡洛大规模验证（10000期） |
| `data.json` | 种子数据（真实开奖，前端自动累积） |
| `.github/workflows/update-data.yml` | 每5分钟自动更新 |

## 快速部署

```bash
# 1. 推送到 GitHub
git init && git add . && git commit -m "V6 增强引擎"
git remote add origin https://github.com/tomf02391-crypto/qianjinalgorithm.git
git push -u origin main

# 2. 开启 Pages
# Settings → Pages → Source: GitHub Actions

# 3. 手动跑一次 Actions
# Actions → 更新PC28数据快照 → Run workflow
```

## 独立运行预测

```bash
cd qianjinalgorithm
python3 predict_v6.py
```

输出示例：
```
下一期预测（基于10期）
  🔪 杀组:    小双
  ✅ 双组押:  小单 + 大双
  🎯 特码5个: [8, 9, 10, 11, 12]
  🔪 杀特码5: [10, 12, 13, 15, 11]
  📊 和值中心: 8 (区间 [6, 10])
  📈 置信度:   55%
```

## ⚠️ 重要提醒

**赌博有害，理性对待。** 本工具仅供算法研究参考，不构成任何投注建议。
PC28 是基于 BCLK Keno 的衍生玩法，开奖结果近似均匀分布，任何声称"稳赚"的工具都是骗局。

## 数据源

- 主源: `https://pc28.help/api/keno.json`
- 备用: `https://yu28.top/api/keno.json`
- 所有数据均来自 BCLC（加拿大不列颠哥伦比亚省彩票公司）官方开奖
